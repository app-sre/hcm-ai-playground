apiVersion: v1
kind: Secret
metadata:
  name: openshell-providers-tenant-test
  namespace: tenant-test
  labels:
    app: openshell
    team: tenant-test
type: Opaque
stringData:
  # Anthropic/Claude credentials
  ANTHROPIC_API_KEY: "REPLACE_WITH_REAL_KEY"

  # GitHub credentials
  GITHUB_TOKEN: "REPLACE_WITH_REAL_TOKEN"

  # Add more provider credentials as needed
  # OPENAI_API_KEY: "sk-..."
  # NVIDIA_API_KEY: "nvapi-..."
