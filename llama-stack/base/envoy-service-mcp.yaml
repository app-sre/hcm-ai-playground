apiVersion: v1
kind: Service
metadata:
  labels:
    app: envoy-mcp
  name: envoy-mcp
  namespace: llama-stack
spec:
  ports:
  - name: web
    port: 8000
    protocol: TCP
  selector:
    app: envoy-mcp